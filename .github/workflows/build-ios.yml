name: Build iOS IPA

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'apps/mobile/**'
      - '.github/workflows/build-ios.yml'

jobs:
  build-ios:
    name: Build iOS (Simulator IPA)
    runs-on: macos-15
    permissions:
      contents: write
    env:
      APP_VARIANT: production
      NODE_OPTIONS: --max-old-space-size=8192
      EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version-file: package.json

      - name: Enable corepack
        run: corepack enable

      - name: Install pnpm
        run: |
          pnpm_version="$(node --print "require('./package.json').packageManager.split('@').pop()")"
          corepack prepare "pnpm@$pnpm_version" --activate
          pnpm --version

      - name: Install dependencies
        run: pnpm install --ignore-scripts

      - name: Setup EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
          packager: npm

      - name: Prebuild iOS
        working-directory: apps/mobile
        run: |
          npx expo prebuild --clean --platform ios

      - name: Build iOS (Simulator)
        working-directory: apps/mobile/ios
        run: |
          xcodebuild \
            -workspace T3Code.xcworkspace \
            -scheme T3Code \
            -configuration Release \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO | tail -50

      - name: Package IPA
        working-directory: apps/mobile
        run: |
          # Find the built .app
          APP_PATH=$(find ios/build -name "T3Code.app" -type d | head -1)
          if [ -z "$APP_PATH" ]; then
            echo "T3Code.app not found, listing build directory:"
            find ios/build -name "*.app" -type d 2>/dev/null || echo "no .app files found"
            echo "Full build dir:"
            ls -la ios/build/
            exit 1
          fi

          echo "Found app: $APP_PATH"

          # Create IPA
          mkdir -p Payload
          cp -r "$APP_PATH" Payload/
          zip -r T3Code-simulator.ipa Payload/
          rm -rf Payload

          echo "IPA size: $(du -h T3Code-simulator.ipa | cut -f1)"
          ls -la T3Code-simulator.ipa

      - name: Upload IPA Artifact
        uses: actions/upload-artifact@v4
        with:
          name: T3Code-simulator
          path: apps/mobile/T3Code-simulator.ipa
          retention-days: 30
